#include<iostream>
#include<vector>
using namespace std;

void print(const vector<int> &element, const vector<int> &index) {
	for (int idx : index) cout << element[idx] << ' ';
	cout << '\n';
}
void pick(vector<int> ) {

}

int main() {
	int n, m;
	cin >> n >> m;
	
	return 0;
}

